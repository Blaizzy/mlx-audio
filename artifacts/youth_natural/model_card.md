# YouthNaturalLoRA Model Card

Status: no trained adapter promoted.

This branch adds the supervised MLX data/training/evaluation scaffold for an
explicit `YouthNaturalLoRA` adapter. No adapter trained on real youth data is
included in Git, and no checkpoint is automatically selected by age inference.
